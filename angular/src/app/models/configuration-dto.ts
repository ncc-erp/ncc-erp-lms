export interface IConfigurationDto {
    location: string;
    scormLocation: string;
}
export class ConfigurationDto implements IConfigurationDto {
    location: string;
    scormLocation: string;
}

export interface IStorageLocationDto {
    location: string;
    scormLocation: string;
}
export class StorageLocationDto implements IStorageLocationDto {
    location: string;
    scormLocation: string;
}
